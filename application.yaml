# example values
server:
  port: 8081

spring:
  datasource:
    username: root
    password: ajhvfcvep.2011
    url: jdbc:mysql://localhost:3306/search_engine?useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create
    show-sql: true

indexing-settings:
  sites:
    - url: https://radio-t.com
      name: radio-t
    - url: https://podlodka.io
      name: podlodka
